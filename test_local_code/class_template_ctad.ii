# 1 "class_template_ctad.cc"
namespace {

template <typename T>
struct A__ {
  T v;
  explicit A__(T x) : v(x) {}
};

template <typename T>
int f__(T x) {
  return ::A__(x).v;
}

}

int main() { return ::f__(1); }
